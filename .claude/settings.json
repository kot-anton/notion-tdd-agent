{
  "_comment": "Local development MCP config — uses tsx so no build step is needed. Credentials come from .env.",
  "mcpServers": {
    "notion-tdd-agent": {
      "command": "npx",
      "args": ["tsx", "src/index.ts"],
      "env": {}
    }
  }
}
